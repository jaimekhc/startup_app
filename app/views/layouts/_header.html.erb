<header class="navbar navbar-fixed-top">
  <div class="navbar navebar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to "Startup_app", root_path, id: "logo", class: 'brand' %>
        <nav>
          <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li><%= link_to '<i class="icon-home icon-white"></i> Home'.html_safe, root_path %></li>
              <li><%= link_to '<i class="icon-th-list icon-white"></i> Jobs'.html_safe, '#' %></li>
              <li><%= link_to '<i class="icon-info-sign icon-white"></i> Help'.html_safe,    '#' %></li>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user icon-white"></i>
                   Account <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Profile', current_user %></li>
                  <li><%= link_to 'Edit my account', edit_user_registration_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to 'Log out', destroy_user_session_path, method: "delete" %>
                  </li>
                </ul>
              </li>              
            <% else %>
              <li><%= render 'layouts/log_in_form_inline' %></li>
              <li><%= link_to "Register|註冊", new_user_registration_path, class: 'transition' %></li>
            <% end %>
            
          </ul>
        </nav>
      </div>
    </div>
  </div>
</header>